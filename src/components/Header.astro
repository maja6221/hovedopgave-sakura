<html lang="da">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="stylesheet" href="../styles/mutual.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
    <!-- font raleway -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;400;500&family=Raleway:wght@200&display=swap" rel="stylesheet" />

    <!-- font commuters sans -->
    <link rel="stylesheet"  href="https://use.typekit.net/jcj6agx.css"/>
    <title>Header</title>
  </head>
</html>
<body>
  <header id="grid_menu">
    <div class="logo">
      <a href="/"><img src="/images/logo_smal.png" alt="logo" /></a>
    </div>

    <button class="toggle-btn">MENU</button>
    <nav id="header-nav">
      <ul class="nav-menu kategorier">
        <li class="nav-item">
          <a href="/shopalt" class="nav-link">Shop alt</a>
        </li>
        <li class="nav-item">
          <a href="/matcha" class="nav-link">Matcha</a>
        </li>
        <li class="nav-item">
          <a href="/eventside" class="nav-link">Events</a>
        </li>

        <div class="icons">
          <img class="sog" src="/images/search.webp" alt="person" />
          <img class="kurv" src="/images/cart.webp" alt="kurv" />

        </div>
      </ul>
    </nav>
  </header>

  <script>
    // ----------------------burgermenu-------------------------

    // En variabel, der refererer til ".toggle-btn"
    const btn = document.querySelector(".toggle-btn");
    // En variabel, der refererer til "nav"
    const nav = document.querySelector("nav");

    // En function, der hedder toggleMenu()
    function toggleMenu() {
      // Toggle klasse "shown", på nav (ved hjælp af classList.toggle)
      nav.classList.toggle("shown");

      // En variabel "menuShown", hvor nav-variblen indeholder klassen "shown" (classList.contains)
      const menuShown = nav.classList.contains("shown");

      // En if/else sætning, der spørger om if (menu)
      if (menuShown) {
        // hvis nav har klassen "shown", er btn.textContent sat til "Luk"
        console.log("luk");
        btn.textContent = "Luk";
      } else {
        // hvis IKKE nav har klassen "shown", er btn.textContent sat til "Menu"
        btn.textContent = "Menu";
        console.log("menu");
      }
    }

    /* Et klik-event til btn, der sætter toggleMenu-funktionen i gang */
    btn.addEventListener("click", toggleMenu);
  </script>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    img {
      width: 100%;
      height: auto;
    }

    ul.nav-menu li {
      display: inline;
    }

    header {
      background-color: var(--Semantik-Baggrund-Beige);
      top: 0;
      padding: 1rem 2rem;
      padding: 10px;
      padding-left: 10px;
      overflow: hidden;
      padding-bottom: 20px;
      padding-top: 5px;
    }

    a {
      text-decoration: none;
      font-weight: 700;
      color: var(--Primitives-Farver-Hvid-sort-09);
      font-family: "didotltstd-roman";
      padding: 9px;
      font-size: 15px;
      /* font-weight: 800; */
      letter-spacing: 1px;
    }

    a:hover {
      color: var(--Semantik-Knap-Red);
    }

    .nav-menu {
      font-family: "didotltstd-roman";
      letter-spacing: 1px;
      gap: 60px;
    }

    #header-nav {
      padding-top: 22px;
    }

    .icons {
      float: right;
      margin-right: 1.5px;
      display: grid;
      grid-template-columns: 1fr 1fr;
    }

  

    .kurv {
      width: 20%;
      grid-column: 4;
      cursor: pointer;
      padding-top: 5px;
    }

    .sog {
      width: 60%;
      grid-column: 3;
      cursor: pointer;
    

    }

    .flag {
      width: 20%;
      grid-column: 5;
      cursor: pointer;
    }

    .kategorier {
      margin-left: 1,5%;
      margin-right: 1.5%;
    }

    .logo {
      width: 10%;
      display: flex;
      align-items: center;
      float: left;
      padding-top: 2px;
      margin-left: 1.5%;
    }
    .toggle-btn {
      display: none;
    }

    #grid_menu {
      align-items: center;
    }

    @media (max-width: 1350px) {
      .kategorier {
        margin-left: 7.5%;
        padding: 10px;
        padding-top: 4px;
      }
      .toggle-btn {
        background: transparent;
        border: none;
        display: block;
        height: 44px;
        padding: 1rem;
        cursor: pointer;
        font-size: 23px;
        color: var(--Primitives-Farver-Hvid-sort-09);
        float: right;
      }

      .toggle-btn:hover {
        color: var(--Semantik-Knap-Red);
      }

      nav {
        display: none;
      }

      nav.shown {
        border-top: 1px solid var(--Primitives-Farver-Hvid-sort-09);
        display: flex;
        flex-flow: column;
        margin-top: 20%;
      }

      nav ul {
        display: flex;
        flex-flow: column;
      }

      .icons {
        margin-left: 10%;
      }

      .logo {
        width: 25%;
        padding-top: 8px;
      }

      #sogefelt {
        margin-left: -31%;
      }
    }

    /* ipad/tablet version */
  </style>
</body>
